# MODULE #40: SEARCH ENGINE
Full-text search with TF-IDF ranking.
```python
from search import SearchEngine
search = SearchEngine()
search.add_document("doc1", "text content here")
results = search.search("query text", top_k=10)
```
**#40 COMPLETE** ✅
