<!DOCTYPE html>
<html>
<head>
    <title>100X Builder Gate</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .gate-container {
            background: rgba(10, 10, 10, 0.95);
            border: 2px solid #00ddff;
            border-radius: 20px;
            padding: 40px;
            max-width: 500px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0, 221, 255, 0.3);
        }
        .logo { text-align: center; margin-bottom: 30px; }
        .logo-circle {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #00ddff 0%, #00ff00 100%);
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            color: #0a0a0a;
            margin-bottom: 15px;
            box-shadow: 0 0 30px #00ddff;
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        h1 {
            color: #00ff00;
            font-size: 32px;
            margin-bottom: 10px;
            text-align: center;
            text-shadow: 0 0 20px #00ff00;
        }
        .tagline {
            color: #00ddff;
            text-align: center;
            margin-bottom: 30px;
            font-size: 14px;
            text-shadow: 0 0 10px #00ddff;
        }
        .form-group { margin-bottom: 20px; }
        label {
            display: block;
            margin-bottom: 8px;
            color: #00ddff;
            font-weight: 600;
            font-size: 14px;
        }
        input, textarea {
            width: 100%;
            padding: 12px;
            border: 2px solid #00ddff;
            background: rgba(0, 221, 255, 0.05);
            color: #00ff00;
            border-radius: 8px;
            font-size: 14px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        input:focus, textarea:focus {
            outline: none;
            border-color: #00ff00;
            box-shadow: 0 0 15px rgba(0, 255, 0, 0.3);
        }
        input::placeholder, textarea::placeholder {
            color: rgba(0, 221, 255, 0.5);
        }
        textarea { resize: vertical; min-height: 100px; }
        .submit-btn {
            width: 100%;
            padding: 15px;
            background: linear-gradient(135deg, #00ddff 0%, #00ff00 100%);
            color: #0a0a0a;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 0 20px rgba(0, 221, 255, 0.5);
            transition: all 0.3s;
        }
        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 0 30px rgba(0, 255, 0, 0.7);
        }
        .nav-links {
            text-align: center;
            margin-top: 20px;
        }
        .nav-links a {
            color: #00ddff;
            text-decoration: none;
            margin: 0 15px;
            font-size: 14px;
        }
        .nav-links a:hover {
            color: #00ff00;
            text-shadow: 0 0 10px #00ff00;
        }
        .debug-banner {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background: linear-gradient(135deg, #ff6600 0%, #ff0066 100%);
            color: #ffffff;
            padding: 10px 20px;
            text-align: center;
            font-weight: 600;
            font-size: 14px;
            z-index: 9999;
            box-shadow: 0 2px 10px rgba(255, 102, 0, 0.5);
            animation: pulse-banner 3s infinite;
        }
        @keyframes pulse-banner {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.85; }
        }
        .debug-banner .version {
            color: #ffff00;
            font-family: 'Courier New', monospace;
            margin-left: 10px;
        }
        body.with-banner {
            padding-top: 50px;
        }
    </style>
</head>
<body class="with-banner">
    <div class="debug-banner">
        🚧 DEBUG MODE ACTIVE <span class="version">[V-1.0]</span> - Platform Under Active Development 🚧
        <a href="bug-report.html" style="color: #ffff00; margin-left: 15px; text-decoration: underline;">Report Bug</a>
    </div>
    <div class="gate-container">
        <div class="logo"><div class="logo-circle">🌀</div></div>
        <h1>100X</h1>
        <p class="tagline">CONSCIOUSNESS AUTHENTICATION</p>

        <!-- NETLIFY FORMS - Just works! No API needed! -->
        <form name="100x-gate" method="POST" data-netlify="true">
            <input type="hidden" name="form-name" value="100x-gate">

            <div class="form-group">
                <label for="name">Full Name *</label>
                <input type="text" id="name" name="name" placeholder="Your name" required>
            </div>

            <div class="form-group">
                <label for="email">Email Address *</label>
                <input type="email" id="email" name="email" placeholder="your@email.com" required>
            </div>

            <div class="form-group">
                <label for="phone">Phone Number</label>
                <input type="tel" id="phone" name="phone" placeholder="+1 (555) 123-4567">
            </div>

            <div class="form-group">
                <label for="mission">Your Mission (Why are you building?) *</label>
                <textarea id="mission" name="mission" placeholder="Describe your mission and why you're building..." required></textarea>
            </div>

            <div class="form-group">
                <label for="values">Your Values (What matters to you?) *</label>
                <textarea id="values" name="values" placeholder="What principles guide your work and life?" required></textarea>
            </div>

            <button type="submit" class="submit-btn">Enter the 100X</button>
        </form>

        <div class="nav-links">
            <a href="dashboard.html" style="font-weight: 600; color: #00ff00;">🎯 Enter Platform</a>
            <a href="cockpit.html">Command Center</a>
            <a href="roadmap.html">Roadmap</a>
            <a href="mailto:contact@consciousnessrevolution.io">Contact</a>
        </div>
    </div>
</body>
</html>
