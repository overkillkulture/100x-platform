# 100X PLATFORM - ENVIRONMENT VARIABLES

# Server
PORT=3100
NODE_ENV=production

# Database (PostgreSQL)
DATABASE_URL=postgres://username:password@host:5432/100x_platform

# Redis (Sessions & Caching)
REDIS_URL=redis://localhost:6379

# Security
SESSION_SECRET=change-this-to-random-32-character-string
ALLOWED_ORIGINS=https://100x.app,https://www.100x.app

# For local development, use:
# DATABASE_URL=postgres://localhost:5432/100x_platform
# REDIS_URL=redis://localhost:6379
# NODE_ENV=development

# 🔱 TRINITY AI CONFIGURATION

# Trinity Mode: online, offline, or hybrid
# - online: Always use paid Anthropic API (best quality, costs money)
# - offline: Always use local LLM (free, privacy-first, requires Ollama/LM Studio)
# - hybrid: Smart routing based on task complexity (recommended, saves 70-80% on costs)
TRINITY_MODE=hybrid

# Anthropic API Key (required for 'online' or 'hybrid' mode)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Daily cost limit for hybrid mode (in USD)
# When limit is reached, Trinity automatically switches to offline mode
COST_LIMIT_PER_DAY=5.00

# Local LLM URL (required for 'offline' or 'hybrid' mode)
# Default: http://localhost:11434 (Ollama default port)
# For LM Studio, use: http://localhost:1234/v1
LOCAL_LLM_URL=http://localhost:11434

# 📝 TRINITY SETUP INSTRUCTIONS

# For OFFLINE Trinity (Free, Private):
# 1. Install Ollama: https://ollama.ai
# 2. Run: ollama pull llama3.1
# 3. Set TRINITY_MODE=offline
# 4. No API key needed!

# For ONLINE Trinity (Best Quality):
# 1. Get Anthropic API key: https://console.anthropic.com/
# 2. Set ANTHROPIC_API_KEY=sk-ant-api03-...
# 3. Set TRINITY_MODE=online

# For HYBRID Trinity (RECOMMENDED - 70-80% Cost Savings):
# 1. Set up both offline and online (steps above)
# 2. Set TRINITY_MODE=hybrid
# 3. Set COST_LIMIT_PER_DAY=5.00
# 4. Trinity automatically:
#    - Uses offline for simple tasks (format, list, convert)
#    - Uses online for complex tasks (analyze, strategy, architecture)
#    - Switches to offline when daily budget reached
